{{#if returns}}
  {{#if returns.[0].description~}}
    <strong>Returns</strong>: 
    {{#each returns~}}
      {{#if type~}}
        {{#if type.names}}{{>linked-type-list types=type.names delimiter=" \| " ~}}{{/if}}
        {{~#if description}} - {{{md (inlineLinks description)}}}{{/if~}}
      {{else~}}
        {{{md (inlineLinks description)}~}}
      {{/if~}}
    {{~/each}}
    <br/>
  {{/if}}
{{/if}}